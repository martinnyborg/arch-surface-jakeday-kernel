version: 2
jobs:
  build:
    docker:
      - image: martinnyborg/archlinux-kernel-builder-docker
    steps:
      - checkout
      - run:
          name: Running makepkg
          command: |
            makepkg -s
      - store_artifacts:
          path: /home/builduser/project
